<h1>Here are the students</h1>
<% @students.each do |student| %>
  <div>
    <%= link_to student.name, house_student_path(student.house, student) %> - <%= House.find(student.house).name %>
<% end %>
  </div>